---
'@chainlink/kaiko-adapter': minor
---

Kaiko updated its API so all requests can now be routed to the same endpoint. This change now routes quote requests in 'ETH' to the same endpoint as all other requests.

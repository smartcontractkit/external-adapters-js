{
  "name": "lcx-api",
  "version": "0.1.0",
  "license": "MIT",
  "main": "index.js",
  "scripts": {
    "server": "node -e 'require(\"./index.js\").server()'",
    "test": "yarn mocha --timeout 0"
  },
  "dependencies": {
    "@chainlink/ea-bootstrap": "0.0.1",
    "@chainlink/external-adapter": "^0.2.3",
    "mocha": "^8.1.3"
  }
}

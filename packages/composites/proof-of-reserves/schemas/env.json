{
  "$id": "https://external-adapters.chainlinklabs.com/schemas/proof-of-reserves-adapter.json",
  "title": "@chainlink/proof-of-reserves-adapter env var schema",
  "required": [],
  "type": "object",
  "properties": {
    "ADAPTER_URL_WBTC": {
      "type": "string"
    },
    "ADAPTER_URL_RENVM": {
      "type": "string"
    },
    "ADAPTER_URL_AMBERDATA": {
      "type": "string"
    },
    "ADAPTER_URL_BLOCKCHAIN_COM": {
      "type": "string"
    },
    "ADAPTER_URL_BLOCKCYPHER": {
      "type": "string"
    },
    "ADAPTER_URL_BLOCKCHAIR": {
      "type": "string"
    },
    "ADAPTER_URL_BTC_COM": {
      "type": "string"
    },
    "ADAPTER_URL_CRYPTOAPIS": {
      "type": "string"
    },
    "ADAPTER_URL_SOCHAIN": {
      "type": "string"
    },
    "ADAPTER_URL_ADA_BALANCE": {
      "type": "string"
    },
    "ADAPTER_URL_CHAIN_RESERVE_WALLET": {
      "type": "string"
    }
  },
  "allOf": [
    {
      "anyOf": [
        {
          "required": ["ADAPTER_URL_AMBERDATA"]
        },
        {
          "required": ["ADAPTER_URL_BLOCKCHAIN_COM"]
        },
        {
          "required": ["ADAPTER_URL_BLOCKCYPHER"]
        },
        {
          "required": ["ADAPTER_URL_BLOCKCHAIR"]
        },
        {
          "required": ["ADAPTER_URL_BTC_COM"]
        },
        {
          "required": ["ADAPTER_URL_CRYPTOAPIS"]
        },
        {
          "required": ["ADAPTER_URL_SOCHAIN"]
        },
        {
          "required": ["ADAPTER_URL_ADA_BALANCE"]
        }
      ]
    }
  ]
}

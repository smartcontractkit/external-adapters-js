{
  "$id": "https://external-adapters.chainlinklabs.com/schemas/coinranking-adapter.json",
  "title": "Chainlink External Adapter for Coinranking",
  "required": ["API_KEY"],
  "type": "object",
  "properties": {
    "API_KEY": {
      "type": "string",
      "description": "An API key that can be obtained from the data provider's dashboard",
      "required": true
    }
  },
  "allOf": [
    {
      "$ref": "https://external-adapters.chainlinklabs.com/schemas/ea-bootstrap.json"
    }
  ]
}

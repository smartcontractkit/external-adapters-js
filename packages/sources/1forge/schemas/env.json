{
  "$id": "https://external-adapters.chainlinklabs.com/schemas/1forge-adapter.json",
  "title": "Chainlink External Adapter for 1forge",
  "description": "##### NOTE: the `price` endpoint is temporarily still supported, however, is being deprecated. Please use the `forex` endpoint instead. [`/convert`](https://1forge.com/api#convert) - Convert from one currency to another",
  "required": ["API_KEY"],
  "type": "object",
  "properties": {
    "API_KEY": {
      "type": "string",
      "description": "An API key that can be obtained from the 1forge dashboard"
    }
  },
  "allOf": [
    {
      "$ref": "https://external-adapters.chainlinklabs.com/schemas/ea-bootstrap.json"
    }
  ]
}

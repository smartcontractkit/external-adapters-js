{
  "$id": "https://external-adapters.chainlinklabs.com/schemas/bitex-adapter.json",
  "title": "Chainlink External Adapter for Bitex",
  "description": "##### NOTE: the `price` endpoint is temporarily still supported, however, is being deprecated. Please use the `crypto` endpoint instead.",
  "required": ["API_KEY"],
  "type": "object",
  "properties": {
    "API_KEY": {
      "type": "string",
      "description": "Bitex API key to use"
    }
  },
  "allOf": [
    {
      "$ref": "https://external-adapters.chainlinklabs.com/schemas/ea-bootstrap.json"
    }
  ]
}

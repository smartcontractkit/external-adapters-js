{
  "$id": "https://external-adapters.chainlinklabs.com/schemas/coinapi-adapter.json",
  "title": "Chainlink CoinApi External Adapter",
  "description": "##### NOTE: the `price` endpoint is temporarily still supported, however, is being deprecated. Please use the `crypto` endpoint instead.",
  "required": ["API_KEY"],
  "type": "object",
  "properties": {
    "API_KEY": {
      "type": "string",
      "required": true,
      "description": "An API key that can be obtained from  [here](https://www.coinapi.io/pricing)"
    }
  },
  "allOf": [
    {
      "$ref": "https://external-adapters.chainlinklabs.com/schemas/ea-bootstrap.json"
    }
  ]
}

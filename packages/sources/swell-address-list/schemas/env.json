{
  "$id": "https://external-adapters.chainlinklabs.com/schemas/swell-address-list-adapter.json",
  "title": "Swell Address List Adapter",
  "description": "This EA fetches the list of custodial addresses that hold the funds for a PoR feed",
  "required": ["RPC_URL"],
  "type": "object",
  "properties": {
    "RPC_URL": {
      "type": "string",
      "description": "The RPC URL to connect to the EVM chain the address manager contract is deployed to."
    }
  },
  "allOf": [
    {
      "$ref": "https://external-adapters.chainlinklabs.com/schemas/ea-bootstrap.json"
    }
  ]
}

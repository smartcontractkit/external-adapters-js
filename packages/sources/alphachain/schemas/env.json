{
  "$id": "https://external-adapters.chainlinklabs.com/schemas/alphachain-adapter.json",
  "title": "Chainlink External Adapter for AlphaChain (SDR)",
  "required": ["API_KEY"],
  "type": "object",
  "properties": {
    "API_KEY": {
      "type": "string",
      "required": true,
      "description": "An API key that can be obtained from the AlphaChain dashboard"
    }
  },
  "allOf": [
    {
      "$ref": "https://external-adapters.chainlinklabs.com/schemas/ea-bootstrap.json"
    }
  ]
}

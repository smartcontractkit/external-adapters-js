{
  "$id": "https://external-adapters.chainlinklabs.com/schemas/coingecko-adapter.json",
  "title": "Chainlink External Adapter for CoinGecko",
  "description": "##### NOTE: the `price` endpoint is temporarily still supported, however, is being deprecated. Please use the `crypto` endpoint instead.",
  "required": [],
  "type": "object",
  "properties": {
    "API_KEY": {
      "type": "string",
      "description": "An optional API key for the Pro API"
    }
  },
  "allOf": [
    {
      "$ref": "https://external-adapters.chainlinklabs.com/schemas/ea-bootstrap.json"
    }
  ]
}

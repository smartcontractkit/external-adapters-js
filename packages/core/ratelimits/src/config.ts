export const DEFAULT_SECOND_RATELIMIT = 2 // used when no tiers are defined for a provider
export const DEFAULT_MINUTE_RATELIMIT = 60 // used when no tiers are defined for a provider
export const BURST_UNDEFINED_QUOTA_MULTIPLE = 2 // used there is a quota limit but not burst limit defined

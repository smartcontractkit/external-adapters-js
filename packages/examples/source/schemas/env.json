{
  "$id": "https://external-adapters.chainlinklabs.com/schemas/{{ADAPTER_NAME_FULL}}.json",
  "title": "{{ADAPTER_NAME_CAPITALIZE}}",
  "description": "{{ADAPTER_NAME_CAPITALIZE}} (this title and description were pulled from source/schemas/env.json)",
  "required": ["API_KEY"],
  "type": "object",
  "properties": {
    "API_KEY": {
      "type": "string",
      "description": "API key to use"
    }
  },
  "allOf": [
    {
      "$ref": "https://external-adapters.chainlinklabs.com/schemas/ea-bootstrap.json"
    }
  ]
}
